<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    style="@style/Block"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/bahamut_titleBarBackground">

    <!-- 時間固定在右側 -->
    <com.kota.telnetUI.textView.TelnetTextViewSmall
        android:id="@+id/ArticleTimeItemView_Time"
        style="@style/TextViewSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true" />

    <!-- IP 固定在左側，但右方限制在時間的左邊 -->
    <com.kota.telnetUI.textView.TelnetTextViewSmall
        android:id="@+id/ArticleTimeItemView_IP"
        style="@style/TextViewSmall"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_toStartOf="@id/ArticleTimeItemView_Time"
        android:layout_marginEnd="8dp"
        android:layout_centerVertical="true"
        android:ellipsize="end"
        android:singleLine="true"
        android:textIsSelectable="true" />
</RelativeLayout>
